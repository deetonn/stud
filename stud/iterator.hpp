#pragma once

#include "forward.hpp"

_STD_API_BEGIN

template <class T>
class iterator {
	//static_assert(false, "stud::iterator<T> must be specialized.");
};

template <class T>
class reverse_iterator {
	//static_assert(false, "stud::reverse_iterator<T> must be specialized.");
};

_STD_API_END